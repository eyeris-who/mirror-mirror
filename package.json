{
  "name": "smart-mirror",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,magenta \"npm --prefix server run dev\" \"wait-on -d 300 tcp:127.0.0.1:3001 && npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^9.1.0"
  }
}
