# Copy to .env and edit.
PORT=3001
WEB_ORIGIN=http://localhost:5173

# ---- Weather (Open-Meteo, no key needed) ----
# Initial location — only a seed. Change it later at /setup (saved to
# server/data/settings.json). Coordinates in decimal degrees.
LATITUDE=40.7128
LONGITUDE=-74.0060
DEFAULT_CITY=New York
DEFAULT_REGION=New York
# "celsius" or "fahrenheit"
TEMPERATURE_UNIT=fahrenheit
# How many days of forecast to show (1-16).
FORECAST_DAYS=7

# ---- Google Calendar ----
# Create an OAuth client at https://console.cloud.google.com/apis/credentials
# (Application type: "Web application"). Enable the Google Calendar API.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback
GOOGLE_CALENDAR_ID=primary

# ---- Spotify ----
# Create an app at https://developer.spotify.com/dashboard
# Add this exact redirect URI in the app settings.
# Playback requires a Spotify Premium account.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3001/api/auth/spotify/callback
